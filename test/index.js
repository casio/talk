var talk = require('..'),
    assert = require('assert')

describe('talk', function() {
  describe('command', function() {
    it('should foo', function() {
      assert(true == false)
    })
  })
})
